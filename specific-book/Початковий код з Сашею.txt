// const id = 1;

	// const book = books.find(value => {
	// 	return value.id === id;
	// })

	// function init() {
	// 	document.getElementById('reduceButton').addEventListener('click', () => updateCountInput(-1));
	// 	document.getElementById('increaseButton').addEventListener('click', () => updateCountInput(1));
	// 	document.getElementById('countInput').addEventListener('change', () => updateCountInput(0));
	// 	// document.querySelector('.item_link').addEventListener('click', goToBookPage); // Додаємо подію на клік на кнопку .item_link
	
	// 	const bookPriceElement = document.getElementById('bookPrice');
	// 	bookPriceElement.textContent = book.price;
	
	// 	const bookAuthorElement = document.getElementById('bookAuthor');
	// 	bookAuthorElement.textContent = book.author;
	
	// 	const bookShortDescriptionElement = document.getElementById('bookShortDescription');
	// 	bookShortDescriptionElement.textContent = book.shortDescription;
	
	// 	const bookLevelElement = document.getElementById('bookLevel');
	// 	bookLevelElement.textContent = book.level;
	
	// 	const bookTitleElement = document.getElementById('bookTitle');
	// 	bookTitleElement.textContent = book.title;
	
	// 	const bookDescriptionElement = document.getElementById('bookDescription');
	// 	bookDescriptionElement.textContent = book.description;
	
	// 	const bookImageElement = document.getElementById('bookImage');
	// 	if (bookImageElement) {
	// 		bookImageElement.setAttribute('src', book.image);
	// 	}
	
	// 	const bookTagsElement = document.getElementById('bookTags');
	// 	for(const tag of book.tags) {
	// 		const linkTag = document.createElement("a");
	// 		linkTag.textContent = '#' + tag;
	// 		bookTagsElement.append(linkTag);
	// 	}
	
	// 	updateTotalPrice(book.price);
	// }
	
	// init();
	
	// function updateTotalPrice(value) {
	// 	const totalPriceElement = document.getElementById('bookTotalPrice');
	// 	totalPriceElement.textContent = value.toFixed(2);
	// } 
	
	// function updateCountInput(increment) {
	// 	const countInput = document.getElementById('countInput');
	// 	const newValue = validateBooksCountRange(+countInput.value + increment); 
	// 	countInput.value = newValue;
	// 	updateTotalPrice(book.price * newValue);
	// }
	
	// function validateBooksCountRange(value) {
	// 	let validValue = Math.max(1, Math.min(42, value));   
	// 	return parseFloat(validValue.toFixed(2));
	// }